{
  "name": "saas-physics",
  "version": "0.4.0",
  "private": true,
  "description": "Prototype 0 — deterministic SaaS economic engine and inspection interface",
  "scripts": {
    "check": "node checks.js",
    "basis": "node basis-checks.js",
    "scenarios": "node scenarios.js",
    "build": "node build.js",
    "state": "node state-sufficiency.js",
    "report": "node checks.js && node opening-checks.js && node cash-checks.js && node billings-checks.js && node expansion-checks.js && node logo-checks.js && node age-checks.js && node notebook-checks.js && node cohorts-checks.js && node basis-checks.js && node state-sufficiency.js && node scenarios.js"
  }
}
